import { styled } from '@mui/system';

const StyledOverview = styled(`div`)({
  maxWidth: 1400,
  margin: `auto`,
});

export default function Overview() {
  return <StyledOverview></StyledOverview>;
}
