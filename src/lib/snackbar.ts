import { SnackbarOrigin } from 'notistack';

export const SNACKBAR_CLASSES = {
  variantInfo: `notistack-variant_info`,
  variantSuccess: `notistack-variant_success`,
};

export const SNACKBAR_DEFAULT_ANCHOR_ORIGIN: SnackbarOrigin = {
  vertical: `top`,
  horizontal: `center`,
};
